import { AppEventBlockerDirective } from './app-event-blocker.directive';

describe('AppEventBlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppEventBlockerDirective();
    expect(directive).toBeTruthy();
  });
});
